// No solidity compilation happens here — deploy-safe.mjs deploys the
// precompiled bytecode shipped inside @safe-global/safe-contracts, so this
// config only needs to describe the network, not a compiler.
export default {};
